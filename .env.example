# TinyTTS Environment Variables

# Required: LiteLLM API key
LITELLM_API_KEY=sk-your-api-key-here

# LiteLLM endpoint (default: http://localhost:4000)
LITELLM_BASE_URL=https://ai.sudiptadhara.in

# TTS Model (default: tts-kokoro)
TTS_MODEL=tts-kokoro

# Default voice (Kokoro TTS voices: af_alloy, af_nova, af_bella, af_sarah, af_sky, am_echo, am_onyx, am_adam, bm_fable, bm_george)
DEFAULT_VOICE=af_alloy

# Default speed (range: 0.25 - 4.0)
DEFAULT_SPEED=1.0

# Maximum storage in GB before cleanup (default: 10)
MAX_STORAGE_GB=10

# Data directory for storage (default: /data)
DATA_DIR=/data

# Chunking settings
INITIAL_CHUNK_SIZE=4000
MIN_CHUNK_SIZE=500
LARGE_INPUT_WARNING=100000

# Server settings
HOST=0.0.0.0
PORT=4040
